临时文档：

1- 先默认电路nLoop=1，无短路与开路的特殊情况，将器件数目转化为1个独立电路。 *不能出现电压源并联及电流源串联的情况，利用并查集完成节点的布置。
2- 保持nLoop=1，加上电源的短路及开路的情况。短路对数需要小于等于电源的个数，相当于把nShort对电源两端节点统一为一个节点；开路bOpen=1即把这个独立电路写成一个环路（一个器件连一个）最后两端不封闭就可以了
3- 再加入nLoop的数据：在一开始先对每个独立电路进行分配保证每个独立电路下：(开路+短路数)<=电源数目，可能还有其他限制条件
4- 加工：可以对一些无法完成的电路条件进行报错，比如电源数为1但是又要短路又有开路这样这无法完成的情况进行报错处理。（optional）
